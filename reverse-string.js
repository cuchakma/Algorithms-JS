let s = ["h", "e", "l", "l", "o"];

// var reverseString = function(s) {
//     if( s.length != 0 ) {
//         let length = s.length - 1;
//         s = s.slice(0, length);
//         return reverseString(s);
//     } else {
//         return s[0];
//     }
// };

// reverseString(s);